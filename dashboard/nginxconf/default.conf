server {
    listen 80 default_server;

    server_name kuberhaus;
    root   /usr/share/nginx/html;

    location / {
        try_files $uri $uri/ /index.html?/$request_uri;
    }
    location /api/ {
        proxy_pass http://kuberhaus-service:8282;
    }

}
